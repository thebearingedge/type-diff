{
  "name": "type-diff",
  "description": "Simple, readable, structural type comparisons",
  "version": "3.1.1",
  "main": "dist/index.js",
  "scripts": {
    "lint": "eslint src/",
    "test": "mocha -r babel-register src/*.test.js",
    "tdd": "npm t -s -- -w -R min",
    "coverage": "babel-node node_modules/.bin/babel-istanbul cover _mocha --",
    "cover": "rimraf coverage/ && run-s -s 'coverage {@}' -- src/*.test.js",
    "transpile": "rimraf dist/ && babel -d dist/ --ignore *.test.js src/",
    "precommit": "run-s -s lint cover transpile"
  },
  "files": [
    "dist/"
  ],
  "engines": {
    "node": ">=6.3.1",
    "npm": ">=3.10.3"
  },
  "keywords": [
    "object",
    "schema",
    "types",
    "nested",
    "structure"
  ],
  "author": "Tim Davis",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/thebearingedge/type-diff.git"
  },
  "devDependencies": {
    "babel-cli": "6.26.0",
    "babel-eslint": "8.0.0",
    "babel-istanbul": "0.12.2",
    "babel-plugin-transform-export-extensions": "6.22.0",
    "babel-preset-node6": "11.0.0",
    "eslint": "4.6.0",
    "chai": "4.1.2",
    "coveralls": "2.13.2",
    "global": "4.3.2",
    "mocha": "6.1.0",
    "husky": "0.13.4",
    "rimraf": "2.6.2",
    "npm-run-all": "4.1.0"
  },
  "dependencies": {
    "lodash": "^4.17.2"
  }
}
